# Bolt's Journal

## 2024-05-22 - [Initial Setup]
**Learning:** Verified project structure and set up initial journal.
**Action:** Always check for this file first.

## 2024-05-22 - [Lazy Loading Modals]
**Learning:** `next/dynamic` works seamlessly with `ssr: false` for client-only modals. Prefetching on hover (`onMouseEnter`) is a crucial UX pattern to avoid load delays.
**Action:** Apply this pattern to all heavy, initially hidden interactive components.
